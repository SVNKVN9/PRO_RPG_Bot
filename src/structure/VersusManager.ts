import { EventEmitter } from 'events'

export default class extends EventEmitter {
    // public VersusMap: Map<string, string>

    constructor() {
        super()
        // this.VersusMap = new Map()
    }
}