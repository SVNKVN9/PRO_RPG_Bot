import Client from "../../structure/Client";

export default {
    name: 'debug',
    execute: (client: Client, info: string) => {
        // console.log(info)
        // client.log.Debug('🟢', info)
    }
}